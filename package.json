{
  "name": "mah-score",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "tsc -b --pretty false",
    "lint": "eslint .",
    "test": "npm run test --workspaces --if-present",
    "format": "prettier --write .",
    "check": "npm run typecheck && npm run lint",
    "check:replay": "npm run build -w shared && node shared/scripts/check-replay.mjs",
    "check:room-lock": "node backend/scripts/check-room-lock.mjs",
    "check:acceptance": "npm run build && npm run check:replay && npm run check:room-lock",
    "check:deploy": "npm run check && npm run test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "eslint": "^9.18.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.20.0"
  }
}
