# 必须放在文件最顶层，指定默认加载的权限组
default_permissions = "project-edit"

# 开启网络权限并允许本地绑定
[permissions.project-edit.network]
enabled = true
allow_local_binding = true

# 具体的域名白名单
[permissions.project-edit.network.domains]
"localhost" = "allow"
"127.0.0.1" = "allow"
